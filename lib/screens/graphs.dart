import 'package:flutter/material.dart';

class Graphs extends StatelessWidget {
  const Graphs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Graphs screen");
  }
}
